type Product {
  id: ID!
  title: String!
  sku: String!
  slug: String!
  subtitle: String
  description: String
  shortDescription: String
  price: Float!
  discountedPrice: Float
  images: [String]!
  tags: [String]
  reviews: [Review]
}

input Paginate {
  page: Int!
  itemsPerPage: Int!
}

type PaginateRes {
  products: [Product!]!
  totalPages: Int
  count: Int
}

type Query {
  products(paginate: Paginate): PaginateRes!
  product(slug: String!): Product!
}
services:
  db-furniro:
    image: 'mongo'
    hostname: mongodb
    ports:
      - 27017:27017
    env_file:
      - ./.env.local
    volumes:
      - db-furniro-data:/data/db

volumes:
  db-furniro-data: